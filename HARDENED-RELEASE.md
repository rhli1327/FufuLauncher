# 加固版同步与发布

本流程适用于 `rhli1327/FufuLauncher` 的加固版。

## 保持上游版本号

主程序、更新器、安装包、内置 `Update.json` 和 Release 标签跟随上游版本号。
本地修复和重新打包不递增上游版本，不另设加固版版本号。
发布说明记录上游提交、修复提交、构建标识和本次修复内容。

## 按构建内容检查更新

是否有更新不能由版本号大小、标签文本或上游更新配置决定。
主程序的更新提示和独立更新器共用 `Shared/ReleaseUpdateClient.cs`：

- 从本加固版仓库获取正式版或预览版发布包。
- 读取发布附件 `release-build.json`，先核验 GitHub 提供的 SHA-256。
- 对照安装目录内同名文件中的构建标识和内容摘要。
- 标识或内容变化且构建时间较新时提供更新；相同构建不重复更新，旧构建不自动覆盖新构建。
- 缺失本地构建信息时允许安装当前发布包。明确选择“回退正式版”时允许回退。
- 版本标签只作界面显示，不参与是否更新的判断。
- 安装包仍必须通过 GitHub 附件 SHA-256 校验，清单或摘要缺失时拒绝更新。

`release-build.json` 由 Windows 工作流在最终二进制和哈希清单生成后创建。
其中的构建标识使用工作流运行 ID 和重试次数，内容摘要覆盖主程序、更新器、核心 DLL、
主插件及完整性清单。该标识不是用户可见的版本号。
**安装包、便携包和 Release 附件必须携带同一份清单。**

## 同步及发布检查

1. 保留更新源、完整性校验和账户保护改动，检查合并后的实际调用流程。
2. 检查主窗口、管理员启动进程和游戏快捷方式。上游的默认 DLL 路径接口可能返回空路径。
3. 运行 `tests/LauncherRegression` 和 `tests/ReleaseUpdateRegression`。
   同版本新构建可更新、相同构建不更新、旧构建不自动降级和损坏文件拒绝加载均须通过。
4. 通过 Windows 工作流中的最终产物检查，核对安装包、便携包、完整性清单及构建清单。
5. 发布附件包括安装包、便携包、`SHA256SUMS.txt` 和 `release-build.json`。
   同版本修复更新现有发布包，在说明中明确记录重建及来源，保持源代码标签与产物对应。
6. 插件下载地址和摘要绑定具体插件产物。如果插件下载包不变，保留原附件和摘要。
7. 发布后核对公开的发布接口、标签、构建清单与所有附件摘要。
   发布说明应区分自动验证与真实游戏实测。

旧版更新器没有构建识别逻辑，无法发现同版本修复。这次需覆盖安装一次；
安装修复包后，后续同版本重建可通过“检查更新”识别。
