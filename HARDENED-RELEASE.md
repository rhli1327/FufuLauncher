# 加固版同步与发布

本流程适用于 `rhli1327/FufuLauncher` 的加固版。

## 版本号

上游版本与加固版版本分别记录。发布说明必须写明所同步的上游版本和提交，
加固版版本号用于安装包标识和更新器比较，不能在同步时直接覆盖成上游版本号。

每次发布前读取本仓库已发布的正式版本。新版本必须同时满足：

- 严格高于此前所有加固版正式版本，不能复用已发布的标签或替换同版本安装包。
- 不低于本次同步的上游版本，以兼容更新器的上游最低版本检查。
- 主程序、更新器、安装包、内置 `Update.json` 和 Release 标签保持一致。

使用四段数字版本号，按数值比较各段，不按字符串排序。
如果上游版本已经高于已发布的加固版，可以采用该上游版本号；否则递增加固版末段。

| 已发布加固版 | 本次同步上游 | 新加固版示例 |
| --- | --- | --- |
| 1.7.0.0 | 1.7.0.0，本地修复 | 1.7.0.1 |
| 1.7.0.1 | 1.7.0.1 | 1.7.0.2 |
| 1.7.0.2 | 1.7.1.0 | 1.7.1.0 |

需同步更新的文件：

- `FufuLauncher/FufuLauncher.csproj`：Version、AssemblyVersion、FileVersion。
- `UpdateFufuLauncher/UpdateFufuLauncher.csproj`：同上。
- `UpdateFufuLauncher/MainWindow.xaml.cs`：AppVersion。
- `FufuLauncher.Installer/setup.iss`：AppVersion、AppVersionNum。
- `FufuLauncher/Update.json`：Version。

插件下载地址和哈希绑定的是具体插件产物，不随启动器版本机械替换。
如果主插件不变，可继续使用已审核的旧 Release 插件包和对应哈希。

## 同步检查

1. 保留本仓库的更新源、完整性校验和账户保护改动，检查合并后的实际调用流程。
2. 同时检查主窗口、管理员启动进程和游戏快捷方式。不要假设上游的默认 DLL 路径接口会返回路径。
3. 更新器应先获取加固版 Release，再与当前安装版本比较；不能先依据上游版本返回“已是最新”。
4. 运行 `tests/LauncherRegression`，并通过 Windows 发布工作流。
   工作流在最终二进制压缩、哈希清单生成后执行插件路径及完整性回归检查。
5. 发布前核对最终安装包、便携包、版本、哈希清单和插件下载哈希。
   Release 应指向通过构建的代码提交。先上传草稿附件并核验，再设为正式最新版。
6. 发布后核对公开的最新版接口、标签和附件下载。发布说明中区分自动验证与真实游戏实测。

1.7.0.0 的旧更新器会漏检上游版本号不变的加固版修复，需要手动覆盖安装一次。
修复后的更新器通过设置中的“检查更新”获取加固版仓库实际发布的版本。
